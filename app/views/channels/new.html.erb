<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge">
	<link rel="stylesheet" href="">
  
</head>
<body>

<%= render 'layouts/nav2' %>

<h1 align="center"><%= t('Create Channels') %></h1>
<div class="d-flex justify-content-center">
<div class="col-md-4 col-md-offset-3">


<%= form_for  @channel, url: {action: "create"} do |form| %>
    <div class="field">
    <%= form.label :channel_name %></a>
    <%= form.text_field :channel_name, name:"channel_name", class: 'form-control'   %>
    </div>
    
<br>
  <div class="field">
  <%= form.label :channel_type %></a>
  <select class="custom-select my-1 mr-sm-2" id="inlineFormCustomSelectPref" name="channel_type">
  <option value="public">PUBLIC</option>
  <option  value="private">PRIVATE</option>
  </select>
  </div>

  <div class="field">    
    <%= form.hidden_field :workspace_id, value: @workspace.id, name:"workspace_id" %>
  </div> 

  <%= form.submit t('Create'), class: 'btn btn-primary btn-sm'  %>      
  
  <% end %>
<br>
<%= button_to t('Cancel'), new_channel_path(:id => @workspace.id), :id => "open-contacts-dialog-btn",
      :class => "btn btn-secondary btn-sm", :method => :get %>
  
</div>
</div>
             

</body>
</html>